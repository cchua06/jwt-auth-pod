apiVersion: v1
kind: Pod
metadata:
  name: jwt-auth-w1
  labels:
    env: test
spec:
  containers:
  - name: jwt-auth-w1
    image: ptrck0321/jwt-auth
    imagePullPolicy: IfNotPresent
  nodeSelector:
    roles: worker