apiVersion: v1
kind: Pod
metadata:
  name: jwt-auth
  labels:
    env: test
spec:
  containers:
  - name: jwt-auth
    image: nginx
    imagePullPolicy: IfNotPresent
  nodeSelector:
    roles: worker
